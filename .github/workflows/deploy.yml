name: 🚀 Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🔐 Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 207.174.215.2 >> ~/.ssh/known_hosts

      - name: 🚀 Deploy via SSH
        run: |
          ssh megamtdj@207.174.215.2 << 'EOF'
            cd /home/megamtdj/apply.manipalschool.edu.in
            git pull origin main
            npm install
            npm run build
            pm2 restart all || pm2 start npm --name "next-app" -- start
          EOF
